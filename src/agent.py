import numpy as np

class Agent:
    def __init__(
        self,
        state_initial,              # initial state
        action_mins,                # minimum action values
        action_maxs,                # maximum action values
        dynamics_model,             # how to estimate the next state from the current state and action
        policy,                     # how to determine the optimal next action
    ):

        pass

